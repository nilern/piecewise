%%

%eop EOF

%pos Pos.t

%term ID of string | EOF
%nonterm expr of CST.expr | program of CST.expr

%name Pcws

%noshift EOF
%verbose

%%

program : expr (expr)

expr : ID (CST.Var (IDleft, Var.Lex (Name.Plain ID)))
